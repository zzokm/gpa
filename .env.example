# Optional base path when serving under a subpath
# NEXT_PUBLIC_BASE_PATH=

# Host port for docker-compose.yml (container listens on 3000)
# PORT=3000

# Set to your GA4 measurement ID (e.g. G-XXXXXXXXXX) in production only
# NEXT_PUBLIC_GA_MEASUREMENT_ID=
